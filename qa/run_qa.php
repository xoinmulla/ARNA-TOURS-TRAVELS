<?php declare(strict_types=1); require_once __DIR__.'/../config/config.php'; require_once __DIR__.'/../config/database.php'; header('Content-Type:text/plain;charset=utf-8'); try{$db=Database::getConnection();echo "PASS | MySQL connection | arna_tours_travels".PHP_EOL;$tables=['admin_users','bookings','customers','destinations','drivers','recommendations','route_searches','services','testimonials','tour_packages','vehicles'];$existing=$db->query('SHOW TABLES')->fetchAll(PDO::FETCH_COLUMN);foreach($tables as $t)echo (in_array($t,$existing,true)?'PASS':'FAIL')." | Table | $t".PHP_EOL;}catch(Throwable $e){echo 'FAIL | MySQL connection | '.$e->getMessage().PHP_EOL;}echo PHP_EOL.'Browser E2E and Lighthouse tests must be run on the WAMP machine.'.PHP_EOL;