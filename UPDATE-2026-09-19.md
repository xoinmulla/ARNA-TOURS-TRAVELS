# Arna Tour & Travels – Update 2026-09-19

## Changes in this build

1. **Tour Packages admin image upload**
   - Replaced the text `Image Path / URL` field in `admin/packages.php` with a real file chooser.
   - Accepts JPG, PNG and WEBP.
   - Maximum upload size: 2 MB.
   - Client-side size validation and server-side MIME/dimension validation.
   - Uploaded images are converted to WebP with randomized filenames.
   - Existing package image is retained when no new file is selected.
   - Existing image is shown while editing a package.

2. **Tour package page navbar**
   - Fixed the white/invisible navbar issue on `tours.php`.
   - Navbar now remains a visible dark ARNA-style navigation bar on the tour package page, including mobile menu and dropdown.

3. **Homepage asymmetric service section**
   - Replaced the previous corporate/AI-oriented storyboard copy with actual Arna services:
     - Taxi & Cab Service
     - Tour Packages
     - Airport Transfers
     - Domestic & International Tours
     - Dandeli Trips
   - Links point to the booking section and tour package categories.

4. **Lightweight tour-page animation**
   - Added a simple entrance transition for the tour hero/package content.

## Validation

- PHP lint: PASS for all PHP files.
- No database schema change is required for these updates.
- Existing tour package categories remain backend-editable.


## Latest homepage/contact update
- Added editable homepage Tour Packages section after Hero.
- Added Admin > Homepage Tours visibility/order controls backed by `tour_packages`.
- Added migration `database/009_home_tour_packages.sql`.
- Updated public business number to +91 94800 01511.
- Added shared WhatsApp and Call floating buttons across public pages.
- Added Instagram, Facebook and YouTube footer icons across public pages.
